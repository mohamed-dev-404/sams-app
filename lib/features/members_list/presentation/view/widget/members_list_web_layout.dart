import 'package:flutter/material.dart';

class MembersListWebLayout extends StatelessWidget {
  const MembersListWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Members List Web Layout'));
  }
}
