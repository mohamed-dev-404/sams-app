import 'package:flutter/material.dart';

class GradesWebLayout extends StatelessWidget {
  const GradesWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Grades Web Layout'));
  }
}
