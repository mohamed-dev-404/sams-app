import 'package:flutter/material.dart';

class LiveSessionsWebLayout extends StatelessWidget {
  const LiveSessionsWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Live Sessions Web Layout'));
  }
}
