import 'package:flutter/material.dart';

class CourseCodeWebLayout extends StatelessWidget {
  const CourseCodeWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
return const Center(child: Text('Course Code Web Layout'));  }
}
