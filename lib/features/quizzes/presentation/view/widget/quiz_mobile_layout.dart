import 'package:flutter/material.dart';

class QuizMobileLayout extends StatelessWidget {
  const QuizMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Quiz Mobile Layout'));
  }
}
