import 'package:flutter/material.dart';

class QuizDetailsWebLayout extends StatelessWidget {
  const QuizDetailsWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('QuizDetailsWebLayout'),
      ),
    );
  }
}
