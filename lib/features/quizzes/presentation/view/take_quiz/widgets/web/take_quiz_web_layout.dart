import 'package:flutter/material.dart';

class TakeQuizWebLayout extends StatelessWidget {
  const TakeQuizWebLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TakeQuizWebLayout'),
      ),
    );
  }
}
